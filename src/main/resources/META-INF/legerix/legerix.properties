# Legerix's own identity, filtered from its pom at build time and read from
# the same source as its payload. Never from the jar manifest: after shading
# that manifest belongs to the consumer (Legerix#21).
legerix.version=${project.version}
tesseract.version=${tesseract.version.long}
leptonica.version=${leptonica.version}
